apiVersion: v1
kind: Service
metadata:
  name: {{ template "fullname" . }}-ws-server
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
spec:
  ports:
  - port: 80
    targetPort: 8000
  selector:
    app: {{ template "fullname" . }}-ws-server
